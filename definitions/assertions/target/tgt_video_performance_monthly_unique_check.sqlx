config {
  type: "assertion",
  description: "Fails if duplicate rows exist for (video_id, month)"
}

SELECT
  t.*,
  COUNT(*) OVER (
    PARTITION BY video_id, month
  ) AS duplicate_count
FROM ${ref("tgt_video_performance_monthly")} t
WHERE (month >= DATE('1900-01-01') OR month IS NULL)
QUALIFY duplicate_count > 1
