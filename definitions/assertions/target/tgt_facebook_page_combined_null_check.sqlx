config {
  type: "assertion",
  description: "Fails if required fields are NULL in tgt_facebook_page_combined"
}

SELECT
  *
FROM ${ref("tgt_facebook_page_combined")}
WHERE (date >= DATE('1900-01-01') OR date IS NULL)
  AND (
    date IS NULL
    OR page_id IS NULL
  )
