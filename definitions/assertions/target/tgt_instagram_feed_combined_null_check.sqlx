config {
  type: "assertion",
  description: "Fails if required fields are NULL in tgt_instagram_feed_combined"
}

SELECT
  *
FROM ${ref("tgt_instagram_feed_combined")}
WHERE (date >= DATE('1900-01-01') OR date IS NULL)
  AND (
    date IS NULL
    OR media_id IS NULL
  )
