config {
  type: "assertion",
  description: "Fails if duplicate rows exist for (date, page_id)"
}

SELECT
  t.*,
  COUNT(*) OVER (
    PARTITION BY date, page_id
  ) AS duplicate_count
FROM ${ref("tgt_facebook_page_combined")} t
WHERE (date >= DATE('1900-01-01') OR date IS NULL)
QUALIFY duplicate_count > 1
