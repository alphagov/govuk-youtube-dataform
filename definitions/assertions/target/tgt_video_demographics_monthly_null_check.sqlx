config {
  type: "assertion",
  description: "Fails if required fields are NULL in tgt_video_demographics_monthly"
}

SELECT
  *
FROM ${ref("tgt_video_demographics_monthly")}
WHERE (month >= DATE('1900-01-01') OR month IS NULL)
  AND (
    video_id IS NULL
    OR month IS NULL
    OR age_group IS NULL
    OR gender IS NULL
    OR data_source IS NULL
  )
