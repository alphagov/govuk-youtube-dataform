config {
  type: "assertion",
  description: "Fails if required fields are NULL in tgt_facebook_reels_combined"
}

SELECT
  *
FROM ${ref("tgt_facebook_reels_combined")}
WHERE (date >= DATE('1900-01-01') OR date IS NULL)
  AND (
    date IS NULL
    OR video_id IS NULL
  )
