
-- A table that contains the yearly output for engagement and content rpport data
config {
  type: "view",
  assertions : {
    uniqueKey: ["video_id","date", "deviceType", "operatingSystem"],
    nonNull: ["video_id","date", "deviceType", "operatingSystem"],
    rowConditions: [
    ]
  },
  columns: {
    video_id: "the video id",
    date: "YYYY-MM-DD date of data",
    deviceType: "device type, TV, tablet, modbile, desktop",
    operatingSystem: "Operating system of device, Amazon_fireOS, Andforid, Chrome_Os, IOS, Machintosh etc",
    engagedViews: "engaged Views where a user has seen the thumbnail/port of the video autoplaying via thumbnail",
    views: "views where the user watches a portion of the video",
    estimatedMinutesWatched: "An estimate of the minutes waches, always rounded",
  }
}

SELECT 
<<<<<<< HEAD
    CAST(video_id AS STRING) AS video_id,
    CAST(day AS DATE) AS date,
    CAST(deviceType AS STRING) AS deviceType,
    CAST(operatingSystem AS STRING) AS operatingSystem,
    CAST(engagedViews AS INT) as engagedViews,
    CAST(views AS INT) AS views,
    CAST(estimatedMinutesWatched AS FLOAT64) AS estimatedMinutesWatched
=======
    video_id,
    CAST(day AS DATE) AS date,
    deviceType,
    operatingSystem,
    engagedViews,
    views,
    estimatedMinutesWatched
>>>>>>> b78d105 (Renamed day to date for all API sources, as this matches the naming used in the data transfer service that we will be using going forward)
FROM
 `gds-social-data.govuk_youtube_channel_staging.operating_system_and_device`
